import { Onest, Permanent_Marker, Trade_Winds } from 'next/font/google'

export const onest = Onest({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400', '500', '600', '700']
})

export const permanentMarker = Permanent_Marker({
  subsets: ['latin'],
  display: 'swap',
  weight: '400'
})

export const tradeWinds = Trade_Winds({
  subsets: ['latin'],
  display: 'swap',
  weight: '400'
})
